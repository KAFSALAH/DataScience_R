#Assigning values for x,y,z variable.
x = 3
y = 5
z = 100
#Assigning the addition output to a variable "ans"
ans <- x+y+z
print(ans)
#The advantage of using 'ans' variable is that now the result of addition is stored in it.
#Therefore, we can call it later when ever it is needed.

